// Superseded by rules/policy.ts (EVIDENCE_SCHEMA_VERSION moved there to sit
// alongside the other automated-pipeline constants it's used with) and by
// evaluate.ts (the automated pipeline's own evidence-fetch-and-persist path).
// Left as an empty module because this environment could not delete files
// during that change - see the completion report for the full removal list.
export {};
