// Superseded by the automated ingestion pipeline (no manual shortlist input
// to validate) and by rules/contracts.ts (the automated pipeline's own
// evaluation-status/reason-code contracts). Left as an empty module because
// this environment could not delete files during that change - see the
// completion report for the full removal list.
export {};
