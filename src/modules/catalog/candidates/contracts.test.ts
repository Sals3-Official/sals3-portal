import { describe, it } from 'vitest';

// contracts.ts (the manual-shortlist Zod schemas) was superseded by the
// automated ingestion pipeline. This test file could not be deleted in this
// environment - see the completion report for the full removal list.
describe.skip('shortlist contracts (superseded, awaiting file deletion)', () => {
  it('no-op', () => {});
});
