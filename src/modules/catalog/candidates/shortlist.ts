// Superseded by the automated ingestion pipeline
// (src/modules/catalog/candidates/ingestion.ts), which creates candidates
// per supplier connection (ADR-008) instead of a manual per-row shortlist
// click. Left as an empty module because this environment could not delete
// files during that change - see the completion report for the full
// removal list.
export {};
