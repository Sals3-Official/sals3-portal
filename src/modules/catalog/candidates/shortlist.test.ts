import { describe, it } from 'vitest';

// shortlist.ts was superseded by the automated ingestion pipeline
// (ADR-008 supplier connections). This test file could not be deleted in
// this environment - see the completion report for the full removal list.
describe.skip('shortlistCandidate (superseded, awaiting file deletion)', () => {
  it('no-op', () => {});
});
